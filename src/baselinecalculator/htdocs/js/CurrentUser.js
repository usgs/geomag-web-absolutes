/* global define */
define([], function () {
  'use strict';

  // This file exists to keep the requirejs build process happy.
  // The "CurrentUser" object should be defined by lib/login.inc.php.

  throw new Error('CurrentUser should be defined by lib/login.inc.php');

});
